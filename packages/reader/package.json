{
  "private": true,
  "name": "@announcing/reader",
  "type": "module",
  "scripts": {
    "genWranglerJsonc": "jiti genWranglerJsonc.ts",
    "dev": "vite dev --port 5175",
    "dev:workers": "wrangler dev --ip 0.0.0.0 -c wrangler.local.jsonc -c ../notification/wrangler.local.jsonc --persist-to ../db-local",
    "test": "vitest run",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "playwright": "echo TODO: refactor playwright test",
    "deploy": "wrangler deploy -c wrangler.remote.jsonc",
    "deploy:preview": "wrangler versions upload -c wrangler.remote.jsonc"
  },
  "devDependencies": {
    "@announcing/cloudflare-support": "workspace:*",
    "@announcing/components": "workspace:*",
    "@announcing/db": "workspace:*",
    "@announcing/i18n": "workspace:*",
    "@announcing/notification": "workspace:*",
    "@cloudflare/workers-types": "catalog:",
    "@playwright/test": "catalog:",
    "@sentry/browser": "catalog:",
    "@sentry/sveltekit": "catalog:",
    "@sveltejs/adapter-cloudflare": "catalog:",
    "@sveltejs/kit": "catalog:",
    "@sveltejs/vite-plugin-svelte": "catalog:",
    "@types/js-cookie": "catalog:",
    "firebase": "catalog:",
    "js-cookie": "catalog:",
    "lru-cache": "catalog:",
    "miniflare": "catalog:",
    "svelte": "catalog:",
    "svelte-check": "catalog:",
    "valibot": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "workbox-precaching": "catalog:",
    "workbox-routing": "catalog:",
    "workbox-strategies": "catalog:",
    "wrangler": "catalog:"
  }
}

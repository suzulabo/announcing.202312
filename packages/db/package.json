{
  "name": "@announcing/db",
  "version": "0.0.1",
  "type": "module",
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "!dist/**/*.spec.*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "@libsql/client": "^0.6.2",
    "base-x": "^4.0.0",
    "drizzle-kit": "^0.22.7",
    "drizzle-orm": "^0.31.2",
    "vitest": "^1.6.0"
  }
}

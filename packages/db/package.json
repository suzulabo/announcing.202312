{
  "name": "@announcing/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@libsql/client": "^0.7.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "base-x": "^4.0.0",
    "drizzle-kit": "^0.22.7",
    "drizzle-orm": "^0.31.2"
  },
  "peerDependencies": {
    "@libsql/client": "^0.7.0"
  }
}
